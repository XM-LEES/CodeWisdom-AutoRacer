# Stage-4B controlled-reverse Nav2 Ackermann configuration.
# Use only with:
#   stage4_navigation.launch.py params_file:=<this file> allow_reverse:=true
# It keeps the same footprint, safety zones, topic isolation, and adapter
# contract as stage4_nav2_params.yaml, but enables reverse planning/control.

amcl:
  ros__parameters:
    use_sim_time: false
    alpha1: 0.4
    alpha2: 0.2
    alpha3: 0.2
    alpha4: 0.4
    alpha5: 0.2
    base_frame_id: "base_footprint"
    beam_skip_distance: 0.5
    beam_skip_error_threshold: 0.9
    beam_skip_threshold: 0.3
    do_beamskip: false
    global_frame_id: "map"
    lambda_short: 0.1
    laser_likelihood_max_dist: 2.0
    laser_max_range: 20.0
    laser_min_range: 0.2
    laser_model_type: "likelihood_field"
    max_beams: 60
    max_particles: 2000
    min_particles: 500
    odom_frame_id: "odom"
    pf_err: 0.05
    pf_z: 0.99
    recovery_alpha_fast: 0.0
    recovery_alpha_slow: 0.0
    resample_interval: 1
    robot_model_type: "nav2_amcl::DifferentialMotionModel"
    save_pose_rate: 0.5
    sigma_hit: 0.2
    tf_broadcast: true
    transform_tolerance: 1.0
    update_min_a: 0.2
    update_min_d: 0.25
    z_hit: 0.5
    z_max: 0.05
    z_rand: 0.5
    z_short: 0.05
    scan_topic: scan
    map_topic: map
    first_map_only: false

bt_navigator:
  ros__parameters:
    use_sim_time: false
    global_frame: map
    robot_base_frame: base_footprint
    odom_topic: /odom
    default_nav_to_pose_bt_xml: "$(find-pkg-share autoracer_robot_nav2)/behavior_trees/stage4_navigate_to_pose_goal_update_only.xml"
    default_nav_through_poses_bt_xml: "$(find-pkg-share autoracer_robot_nav2)/behavior_trees/stage4_navigate_through_poses_no_recovery.xml"
    bt_loop_duration: 10
    default_server_timeout: 20
    transform_tolerance: 0.1
    error_code_names:
      - compute_path_error_code
      - follow_path_error_code

controller_server:
  ros__parameters:
    use_sim_time: false
    controller_frequency: 20.0
    odom_topic: /odom
    progress_checker_plugin: "progress_checker"
    goal_checker_plugins: ["goal_checker"]
    controller_plugins: ["FollowPath"]

    progress_checker:
      plugin: "nav2_controller::SimpleProgressChecker"
      required_movement_radius: 0.5
      movement_time_allowance: 10.0

    goal_checker:
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.35
      yaw_goal_tolerance: 0.35
      stateful: true

    FollowPath:
      plugin: "nav2_regulated_pure_pursuit_controller::RegulatedPurePursuitController"
      desired_linear_vel: 0.50
      lookahead_dist: 0.80
      min_lookahead_dist: 0.60
      max_lookahead_dist: 1.20
      lookahead_time: 1.5
      rotate_to_heading_angular_vel: 0.35
      transform_tolerance: 0.1
      use_velocity_scaled_lookahead_dist: true
      min_approach_linear_velocity: 0.18
      approach_velocity_scaling_dist: 1.0
      use_collision_detection: true
      max_allowed_time_to_collision_up_to_carrot: 1.0
      use_regulated_linear_velocity_scaling: true
      use_cost_regulated_linear_velocity_scaling: true
      cost_scaling_dist: 0.40
      cost_scaling_gain: 0.70
      inflation_cost_scaling_factor: 3.0
      regulated_linear_scaling_min_radius: 2.24
      regulated_linear_scaling_min_speed: 0.18
      use_rotate_to_heading: false
      allow_reversing: true
      rotate_to_heading_min_angle: 0.785
      max_angular_accel: 1.0
      max_robot_pose_search_dist: 3.0

local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 5.0
      publish_frequency: 2.0
      global_frame: odom
      robot_base_frame: base_footprint
      use_sim_time: false
      rolling_window: true
      width: 3
      height: 3
      resolution: 0.05
      footprint: "[[-0.16,-0.285],[-0.16,0.285],[0.7175,0.285],[0.7175,-0.285]]"
      plugins: ["obstacle_layer", "inflation_layer"]
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: true
        observation_sources: scan
        scan:
          topic: /scan
          max_obstacle_height: 2.0
          clearing: true
          marking: true
          data_type: "LaserScan"
          raytrace_max_range: 10.0
          raytrace_min_range: 0.0
          obstacle_max_range: 3.0
          obstacle_min_range: 0.0
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 0.55
      always_send_full_costmap: true
  local_costmap_client:
    ros__parameters:
      use_sim_time: false
  local_costmap_rclcpp_node:
    ros__parameters:
      use_sim_time: false

global_costmap:
  global_costmap:
    ros__parameters:
      update_frequency: 1.0
      publish_frequency: 1.0
      global_frame: map
      robot_base_frame: base_footprint
      use_sim_time: false
      footprint: "[[-0.16,-0.285],[-0.16,0.285],[0.7175,0.285],[0.7175,-0.285]]"
      resolution: 0.05
      track_unknown_space: true
      plugins: ["static_layer", "obstacle_layer", "inflation_layer"]
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: true
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: true
        observation_sources: scan
        scan:
          topic: /scan
          max_obstacle_height: 2.0
          clearing: true
          marking: true
          data_type: "LaserScan"
          raytrace_max_range: 10.0
          raytrace_min_range: 0.0
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 10.0
        inflation_radius: 0.25
      always_send_full_costmap: true
  global_costmap_client:
    ros__parameters:
      use_sim_time: false
  global_costmap_rclcpp_node:
    ros__parameters:
      use_sim_time: false

map_server:
  ros__parameters:
    use_sim_time: false
    yaml_filename: ""

map_saver:
  ros__parameters:
    use_sim_time: false
    save_map_timeout: 5.0
    free_thresh_default: 0.196
    occupied_thresh_default: 0.65
    map_subscribe_transient_local: true

planner_server:
  ros__parameters:
    planner_plugins: ["GridBased"]
    use_sim_time: false
    expected_planner_frequency: 2.0

    GridBased:
      plugin: "nav2_smac_planner/SmacPlannerHybrid"
      tolerance: 0.5
      downsample_costmap: false
      downsampling_factor: 1
      allow_unknown: false
      max_iterations: 1000000
      max_on_approach_iterations: 1000
      max_planning_time: 5.0
      motion_model_for_search: "REEDS_SHEPP"
      cost_travel_multiplier: 2.0
      angle_quantization_bins: 72
      analytic_expansion_ratio: 3.5
      analytic_expansion_max_length: 10.0
      analytic_expansion_max_cost: 200.0
      minimum_turning_radius: 2.24
      reverse_penalty: 1.5
      change_penalty: 0.0
      non_straight_penalty: 1.20
      cost_penalty: 2.0
      lookup_table_size: 20.0
      cache_obstacle_heuristic: true
      smooth_path: true
      smoother:
        max_iterations: 1000
        w_smooth: 0.3
        w_data: 0.2
        tolerance: 1.0e-10

smoother_server:
  ros__parameters:
    use_sim_time: false
    smoother_plugins: ["simple_smoother"]
    simple_smoother:
      plugin: "nav2_smoother::SimpleSmoother"
      tolerance: 1.0e-10
      max_its: 1000
      do_refinement: true

behavior_server:
  ros__parameters:
    use_sim_time: false
    costmap_topic: local_costmap/costmap_raw
    footprint_topic: local_costmap/published_footprint
    cycle_frequency: 10.0
    behavior_plugins: ["wait"]
    wait:
      plugin: "nav2_behaviors/Wait"
    global_frame: odom
    robot_base_frame: base_footprint
    transform_tolerance: 0.1

waypoint_follower:
  ros__parameters:
    use_sim_time: false
    loop_rate: 20
    stop_on_failure: false
    waypoint_task_executor_plugin: "wait_at_waypoint"
    wait_at_waypoint:
      plugin: "nav2_waypoint_follower::WaitAtWaypoint"
      enabled: true
      waypoint_pause_duration: 200

velocity_smoother:
  ros__parameters:
    use_sim_time: false
    smoothing_frequency: 20.0
    scale_velocities: false
    feedback: "OPEN_LOOP"
    max_velocity: [0.50, 0.0, 0.60]
    min_velocity: [-0.30, 0.0, -0.60]
    max_accel: [0.5, 0.0, 1.0]
    max_decel: [-0.5, 0.0, -1.0]
    odom_topic: /odom
    odom_duration: 0.1
    deadband_velocity: [0.0, 0.0, 0.0]
    velocity_timeout: 1.0

collision_monitor:
  ros__parameters:
    use_sim_time: false
    enabled: true
    base_frame_id: "base_footprint"
    odom_frame_id: "odom"
    cmd_vel_in_topic: "/nav2_cmd_vel"
    cmd_vel_out_topic: "/safe_nav2_cmd_vel"
    state_topic: "/collision_monitor/state"
    transform_tolerance: 0.3
    source_timeout: 1.0
    base_shift_correction: true
    stop_pub_timeout: 2.0
    enable_stamped_cmd_vel: false
    polygons: ["front_stop", "front_slowdown", "rear_stop", "rear_slowdown", "left_side_stop", "right_side_stop"]
    observation_sources: ["scan"]

    front_stop:
      type: "polygon"
      points: [0.72, -0.35, 1.05, -0.35, 1.05, 0.35, 0.72, 0.35]
      action_type: "stop"
      min_points: 4
      max_points: 3
      visualize: true
      polygon_pub_topic: "front_stop_polygon"
      enabled: true

    front_slowdown:
      type: "polygon"
      points: [0.72, -0.45, 1.50, -0.45, 1.50, 0.45, 0.72, 0.45]
      action_type: "slowdown"
      slowdown_ratio: 0.35
      min_points: 4
      max_points: 3
      visualize: true
      polygon_pub_topic: "front_slowdown_polygon"
      enabled: true

    rear_stop:
      type: "polygon"
      points: [-0.55, -0.35, -0.16, -0.35, -0.16, 0.35, -0.55, 0.35]
      action_type: "stop"
      min_points: 4
      max_points: 3
      visualize: true
      polygon_pub_topic: "rear_stop_polygon"
      enabled: true

    rear_slowdown:
      type: "polygon"
      points: [-1.20, -0.45, -0.16, -0.45, -0.16, 0.45, -1.20, 0.45]
      action_type: "slowdown"
      slowdown_ratio: 0.35
      min_points: 4
      max_points: 3
      visualize: true
      polygon_pub_topic: "rear_slowdown_polygon"
      enabled: true

    left_side_stop:
      type: "polygon"
      points: [-0.16, 0.285, 0.72, 0.285, 0.72, 0.45, -0.16, 0.45]
      action_type: "stop"
      min_points: 4
      max_points: 3
      visualize: true
      polygon_pub_topic: "left_side_stop_polygon"
      enabled: true

    right_side_stop:
      type: "polygon"
      points: [-0.16, -0.45, 0.72, -0.45, 0.72, -0.285, -0.16, -0.285]
      action_type: "stop"
      min_points: 4
      max_points: 3
      visualize: true
      polygon_pub_topic: "right_side_stop_polygon"
      enabled: true

    scan:
      type: "scan"
      topic: "/scan"
      enabled: true
      source_timeout: 1.0

lifecycle_manager_localization:
  ros__parameters:
    use_sim_time: false
    autostart: true
    node_names: ["map_server", "amcl"]

lifecycle_manager_navigation:
  ros__parameters:
    use_sim_time: false
    autostart: true
    node_names: ["controller_server", "smoother_server", "planner_server", "behavior_server", "bt_navigator", "waypoint_follower", "velocity_smoother"]

lifecycle_manager_collision_monitor:
  ros__parameters:
    use_sim_time: false
    autostart: true
    node_names: ["collision_monitor"]
