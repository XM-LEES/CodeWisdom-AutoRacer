# 启动与运行规范

本文维护 `CodeWisdom-AutoRacer/` 的 ROS2 启动入口、launch 参数、topic 检查和阶段验收入口。
根目录文档只维护系统约束、阶段目标、接口协议和验证边界。

阶段 1-4 的验收契约、追踪表、PASS/FAIL 规则和人工边界以根目录
`../../docs/阶段路线图.md` 和 `../../docs/开发流程与验证规范.md` 为准。本文只记录上位机可执行入口和 topic/launch 检查；测试结果必须回填根目录阶段验收追踪表。

## 当前可运行启动入口

当前车载上位机路径按 `/home/car/CodeWisdom-AutoRacer` 记录：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
colcon build --symlink-install
source install/setup.bash
```

全量构建要求目标 Jetson 已安装本仓库所有硬件/传感器依赖。当前已知缺口包括
`lslidar_driver` 需要 `libpcap-dev`，ZED 相关包需要 ZED SDK，旧 gmapping 包需要对应源码/头文件完整。
如果开发机缺少这些依赖，只能用 `--packages-select` 或 `--packages-ignore` 验证本轮改动涉及的包；
不得把选择性构建记录为完整上车环境构建 PASS。

## 最终任务入口

最终现场使用只暴露两个任务：建图和导航。阶段 1-4 的 launch 仍保留为研发、验收和排障展开入口，
不作为日常操作界面。

```bash
cd /home/car/CodeWisdom-AutoRacer
tools/runtime/autoracer.sh map
```

```bash
cd /home/car/CodeWisdom-AutoRacer
tools/runtime/autoracer.sh nav --map docs/test-records/maps/stage3-final-floor2-loop-20260517-101119.yaml
```

```bash
cd /home/car/CodeWisdom-AutoRacer
tools/runtime/autoracer.sh preflight nav --map docs/test-records/maps/stage3-final-floor2-loop-20260517-101119.yaml
```

常用选项：

- `--counts <value>`：覆盖当前默认霍尔标定值。当前默认值为 `13.545`，只有重新标定后才需要手动传。
- `--no-rviz`：不启动 RViz。
- `--dry-run`：只打印最终 `ros2 launch ...` 命令，不启动节点。
- `-- <extra launch args>`：把后续参数透传到底层 launch。
- `--skip-preflight`：临时跳过默认启动前检查，只应用于明确排障场景。
- `nav --reverse`：显式切换阶段 4B 倒车候选参数，并同步设置 adapter `allow_reverse:=true`。
- `nav --no-capture`：只启动导航，不启动默认伴生采集。
- `nav --capture-raw-cloud`：默认采集之外额外录 `/point_cloud_raw`，只在需要原始点云证据时使用。

`autoracer.sh` 会自动 source 本仓库的 `source_all.sh`，日常启动不需要人工 source ROS 环境。
导航启动后，在 RViz 里先用 `2D Pose Estimate` 设置初始位姿，再发送目标点；未设置初始位姿前，
AMCL 提示需要 initial pose、Nav2 等待 `map -> odom` 是正常状态。

`autoracer.sh map` 和 `autoracer.sh nav` 默认先运行启动前检查；检查失败时不会继续启动节点。
单独排查时使用 `tools/runtime/autoracer.sh preflight map` 或
`tools/runtime/autoracer.sh preflight nav --map docs/test-records/maps/stage3-final-floor2-loop-20260517-101119.yaml`。
检查结果会打印 PASS/FAIL，并写入：

```text
docs/test-records/artifacts/<run-id>/checks/startup_preflight.json
```

默认检查项包括：工作区是否已构建并可找到 ROS 包、地图 yaml 和地图图像是否存在、磁盘空间是否足够、
底盘串口 `/dev/ttyACM0`、IMU by-id 串口、LiDAR 参数和 `192.168.1.200` 网络连通性。
如果通过 `-- start_imu:=false start_lidar:=false start_chassis:=false` 关闭某个 launch 组件，对应硬件检查也会跳过。

`autoracer.sh nav` 默认启动只读采集，不启动导航目标、不发布运动命令，只记录已经运行的 Stage-4
导航图。一次 `nav` 会话内可以在 RViz 连续发送多个目标；停止 `autoracer.sh nav` 后，采集进程会一起停止并生成摘要。
输出目录为：

```text
docs/test-records/artifacts/<run-id>/
```

其中 `rosbags/<run-id>/` 保存完整 rosbag，`checks/startup_preflight.json` 保存启动前检查，
`checks/preflight.json` 保存采集启动时 topic/node 快照，
`checks/post_summary.json` 保存 abort 分层摘要，`logs/` 保存 topic、node 和 rosbag 记录日志。

`tools/diagnostics/nav_capture.py` 是底层采集实现，日常不需要单独启动。排障时可直接运行：
`tools/diagnostics/nav_capture.py --one-goal` 只观察一次 `/navigate_to_pose`，终态后延迟 `3 s` 停止；
`--duration <sec>` 用于固定时长采集，`--raw-cloud` 或 `--all-topics` 用于扩大采集范围。
该工具也会自动 source `source_all.sh`。

下文每个手动展开的新终端都必须先执行以上 `cd`、`source /opt/ros/humble/setup.bash` 和 `source install/setup.bash`。

phase-1 Ackermann `/odom` 验收当前可运行入口。每个终端必须保持运行。

阶段 4 正式启动优先使用 `tools/runtime/autoracer.sh nav`。该入口默认启动 IMU raw、Madgwick、
URDF/TF、阶段 1 底盘、LiDAR、Nav2、Collision Monitor 和 `twist_to_ackermann`。
下面的分终端命令只保留为排障展开方式。

终端 1，启动 IMU raw 数据：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch hipnuc_imu imu_spec_msg.launch.py
```

终端 2，启动 Madgwick，生成 `/imu/data`：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 run imu_filter_madgwick imu_filter_madgwick_node --ros-args --params-file "$(ros2 pkg prefix turn_on_autoracer_robot)/share/turn_on_autoracer_robot/config/imu.yaml"
```

终端 3，启动 Ackermann 底盘桥和 EKF：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch turn_on_autoracer_robot ackermann_chassis.launch.py counts_per_meter:=<实测值> use_ekf:=true
```

约束：

- `counts_per_meter` 必须大于 `0` 才能验收 `/wheel_odom`。
- 没有 `counts_per_meter` 实测值时，只能做串口、telemetry 和 `/chassis_state` bringup。
- 没有 `counts_per_meter` 实测值时，不得记录 `/wheel_odom` 验收通过。
- `use_ekf:=true` 是 phase-1 `/odom` 验收要求。
- `ackermann_chassis.launch.py` 当前不启动 IMU 驱动。验收 `/odom` 前必须按终端 1 和终端 2 启动 `/imu/data`。
- `ackermann_chassis.launch.py` 会发布 `base_footprint -> gyro_link` 静态 TF，当前按同点同向处理，供 EKF 转换 IMU frame。
- 当前已确认的阶段 1 标定值为 `counts_per_meter:=13.545`；该值来自霍尔 `10` 计数/圈、轮径 `0.235 m`，并经三次大于 `2 m` 的直线采集确认。
- `counts_per_meter` 标定记录必须包含至少 3 次不小于 2 m 的 `hall_delta_count` 累计结果，最大/最小结果相对均值偏差不超过 10%。

phase-1 串口和 telemetry bringup 当前可运行入口：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch turn_on_autoracer_robot ackermann_chassis.launch.py counts_per_meter:=0.0 use_ekf:=false
```

约束：

- 该命令只用于串口、telemetry 和 `/chassis_state` bringup。
- 该命令不得记录 `/wheel_odom` 验收通过。
- 该命令不得记录 `/odom` 验收通过。

当前 LiDAR 入口：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch lslidar_driver lslidar_cx_launch.py
```

当前 C32 LiDAR 固定地址为 `192.168.1.200`，主机有线口应配置在同一网段，现场验证使用
`enp0s31f6=192.168.1.102/24`。若 LiDAR 报 `poll() timeout` 或 `/scan` 无数据，先检查：

```bash
ip -br addr show enp0s31f6
ping -I enp0s31f6 -c 3 -W 1 192.168.1.200
ros2 topic hz /scan
```

没有 `192.168.1.x` 地址、ping 不通 `192.168.1.200` 或 `/scan` 无稳定频率时，不得继续阶段 3/4 跑车。

当前 2D SLAM 入口。phase-1 canonical `/odom` 已启动时，必须关闭内置底盘 include：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch autoracer_slam_toolbox slam.launch.py include_bringup:=false
```

当前地图导航入口。启动前必须已经启动 phase-1 底盘链路和 LiDAR：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch autoracer_robot_nav2 navigation.launch.py map:=docs/test-records/maps/stage3-final-floor2-loop-20260517-101119.yaml
```

## 固定目标启动入口

阶段 1 至阶段 4 固定目标入口已落地。最终用户入口是 `tools/runtime/autoracer.sh map/nav`；
下表中的阶段入口只作为研发、验收和排障展开入口。阶段 3 二楼环形通道 RC/manual 最终地图已有 PASS 记录；
阶段 4 固定地图定位、规划、障碍停车和实车导航仍需继续按阶段记录补齐。

| 阶段 | 固定目标入口 | 当前状态 |
| --- | --- | --- |
| 阶段 1 | `ros2 launch autoracer_bringup stage1_chassis.launch.py counts_per_meter:=<实测值> use_ekf:=true` | 已实现 |
| 阶段 2 | `ros2 launch autoracer_bringup stage2_path_tracking.launch.py counts_per_meter:=<实测值>` | 已实现 |
| 阶段 2 点目标 | `ros2 launch autoracer_bringup stage2_goal_tracking.launch.py counts_per_meter:=<实测值> goal_x_m:=3.0 goal_y_m:=2.0` | 已实现 |
| 阶段 3 | `ros2 launch autoracer_bringup stage3_mapping.launch.py counts_per_meter:=<实测值>` | 已实现，未上车验收 |
| 阶段 4 | `ros2 launch autoracer_bringup stage4_navigation.launch.py map:=docs/test-records/maps/stage3-final-floor2-loop-20260517-101119.yaml counts_per_meter:=<实测值>` | 已实现，未上车验收 |

固定目标入口约束：

- 阶段 1 `stage1_chassis.launch.py` include 当前 `turn_on_autoracer_robot/launch/ackermann_chassis.launch.py`。
- 阶段 2 `stage2_path_tracking.launch.py` include 阶段 1 入口，并启动阶段 2 path publisher 和 Pure Pursuit tracker。
- 阶段 2 `stage2_goal_tracking.launch.py` include 阶段 1 入口，并启动 point-goal path publisher 和 Pure Pursuit tracker；该入口按起始 `odom` 位姿生成固定直线路径，不做避障。
- 阶段 3 `stage3_mapping.launch.py` include IMU raw、Madgwick、URDF/TF、阶段 1 入口、LiDAR 和 `autoracer_slam_toolbox/slam.launch.py include_bringup:=false`。
- 阶段 4 `stage4_navigation.launch.py` include IMU raw、Madgwick、URDF/TF、阶段 1 入口、LiDAR、Nav2 localization/navigation、Collision Monitor 和 `twist_to_ackermann`。
- 任何阶段不得把 legacy `/cmd_vel` 路径写入阶段验收入口。

## 阶段 1：底盘 Ackermann bringup

### 当前状态

当前可运行入口：

终端 1，启动 IMU raw 数据：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch hipnuc_imu imu_spec_msg.launch.py
```

终端 2，启动 Madgwick，生成 `/imu/data`：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 run imu_filter_madgwick imu_filter_madgwick_node --ros-args --params-file "$(ros2 pkg prefix turn_on_autoracer_robot)/share/turn_on_autoracer_robot/config/imu.yaml"
```

终端 3，启动 Ackermann 底盘桥和 EKF：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch turn_on_autoracer_robot ackermann_chassis.launch.py counts_per_meter:=<实测值> use_ekf:=true
```

固定目标入口：

```bash
ros2 launch autoracer_bringup stage1_chassis.launch.py counts_per_meter:=<实测值> use_ekf:=true
```

固定目标入口已实现。`counts_per_meter` 仍必须使用实测值；使用 `0.0` 时不得记录可信 `/wheel_odom` 或 `/odom` PASS。

### 启动节点

当前 `ackermann_chassis.launch.py` 启动：

| 节点 | 条件 | 作用 |
| --- | --- | --- |
| `turn_on_autoracer_robot/ackermann_chassis_bridge`，节点名 `ackermann_chassis_bridge` | 总是启动 | `/ackermann_cmd` 与 UART4 Ackermann v1 command/telemetry 桥接 |
| `robot_localization/ekf_node`，节点名 `ekf_filter_node` | `use_ekf:=true` | `/wheel_odom + /imu/data -> /odom` |

当前入口不启动：

- `hipnuc_imu` IMU 驱动。
- `robot_state_publisher`。
- `joint_state_publisher`；URDF 固定传感器 TF 不依赖该节点，只有关节可视化调试时才按需启动。
- LiDAR。
- SLAM。
- Nav2。
- 路径跟踪节点。

### Topic

`ackermann_chassis_bridge` 必须订阅：

| Topic | 用途 |
| --- | --- |
| `/ackermann_cmd` | 正式 Ackermann 底盘命令；串口下行入口 |

`ackermann_chassis_bridge` legacy 兼容输入：

| Topic | 用途 |
| --- | --- |
| `/cmd_vel` | legacy 兼容输入；禁止用于阶段验收 |

`ackermann_chassis_bridge` 必须发布：

| Topic | 用途 |
| --- | --- |
| `/chassis_state` | STM32 24 字节 telemetry 解码状态 |
| `/wheel_odom` | 基于 `hall_delta_count + steering_angle` 的轮式里程计 |
| `/PowerVoltage` | 电压遥测，参数 `publish_power_voltage:=true` 时发布 |

legacy `/cmd_vel` 兼容规则：

- legacy `/cmd_vel` 只允许作为迁移、手动或排障输入。
- 如兼容转换仍启用，转换结果也必须进入同一个正式 `/ackermann_cmd` 底盘命令 topic；不得建立第二套 legacy 输出 topic。
- 阶段验收记录必须证明命令源来自阶段固定入口，不得把 legacy `/cmd_vel` 兼容转换记为 PASS 证据。

`ekf_filter_node` 必须订阅：

| Topic | 用途 |
| --- | --- |
| `/wheel_odom` | 轮式里程计输入 |
| `/imu/data` | Jetson 侧 IMU 输入 |

`ekf_filter_node` 必须发布：

| Topic | 用途 |
| --- | --- |
| `/odom` | phase-1 canonical odom |
| `/tf` | `odom -> base_footprint` |

### 参数表

`ackermann_chassis.launch.py` 参数：

| 参数 | 当前默认值 | 验收约束 |
| --- | --- | --- |
| `usart_port_name` | `/dev/ttyACM0` | 按车载 STM32 串口设备设置 |
| `serial_baud_rate` | `115200` | 必须与 STM32 UART4 一致 |
| `counts_per_meter` | `0.0` | `/wheel_odom` 验收必须大于 `0` |
| `use_ekf` | `false` | `/odom` 验收必须为 `true` |

`ackermann_chassis_bridge` 配置文件参数：

| 参数 | 当前值 |
| --- | --- |
| `odom_frame_id` | `odom` |
| `robot_frame_id` | `base_footprint` |
| `wheelbase_m` | `0.60` |
| `max_steering_angle_rad` | `0.262` |
| `min_turn_speed_mps` | `0.05` |
| `max_auto_speed_mps` | `1.00` |
| `max_reverse_speed_mps` | `0.60` |
| `publish_power_voltage` | `true` |
| `serial_poll_period_ms` | `5` |
| `serial_reconnect_period_ms` | `1000` |

这些速度限幅对应 STM32 固件默认值 `APP_ORIN_VX_FORWARD_CAP_MMPS=1000` 和
`APP_ORIN_VX_REVERSE_CAP_MMPS=600`。`APP_ORIN_VX_MAX_DEFAULT_MMPS=500` 是旧变量的回退值；
当前显式前进/倒车 cap 非零时不作为有效自动命令上限。
`APP_HALL_SPEED_LIMIT_MMPS=5000` / `APP_HALL_SPEED_LIMIT_RELEASE_MMPS=4500`
是霍尔反馈超速保护阈值，不是自动控制命令限速，也不是阶段 1/2 测试速度目标。

### 启动前检查

```bash
ros2 pkg prefix turn_on_autoracer_robot
ros2 pkg executables turn_on_autoracer_robot
test -e /dev/ttyACM0
python3 tools/acceptance/stage1_acceptance_check.py --mode offline
```

必须确认：

- STM32 固件已烧录。
- UART4 线缆、波特率、共地和电平已确认。
- RC 接管、急停、刹车和断电路径可用。
- `counts_per_meter` 有实测值，或本次只做串口/telemetry bringup。
- `/imu/data_raw` 已由 `hipnuc_imu` 发布，或本次不验收 `/odom`。
- `/imu/data` 已由 `imu_filter_madgwick` 发布，或本次不验收 `/odom`。

`counts_per_meter` 标定：

```text
counts_per_meter = abs(sum(hall_delta_count)) / measured_distance_m
```

记录要求：

- 单次标定距离不小于 `2.0 m`。
- 同一方向至少执行 `3` 次。
- 保存每次 `measured_distance_m`、`sum(hall_delta_count)`、单次结果和均值。
- 最大值和最小值相对均值偏差不超过 `10%`。
- 阶段 1 当前采用 `counts_per_meter=13.545`，来源为霍尔 `10` 计数/圈和轮径 `0.235 m`，并经三次大于 `2 m` 的直线采集确认。
- 无标定记录时不记录 `/wheel_odom` 或 `/odom` PASS。

### 启动后检查

```bash
ros2 node list
ros2 topic list
ros2 topic echo /chassis_state --once
ros2 topic hz /chassis_state
ros2 topic hz /wheel_odom
ros2 topic hz /odom
ros2 run tf2_ros tf2_echo odom base_footprint
python3 tools/acceptance/stage1_acceptance_check.py --mode live
python3 tools/acceptance/stage1_acceptance_check.py --mode live --require-odom
```

必须检查：

- `/chassis_state` 有数据。
- `/wheel_odom` 在 `counts_per_meter>0` 且 telemetry 有效时发布。
- `/odom` 在 `use_ekf:=true` 且 `/imu/data`、`/wheel_odom` 有效时发布。
- 通信超时后 STM32 进入停车状态。
- 非零运动命令测试必须满足架空或安全场地条件。

验收工具边界：

- `stage1_acceptance_check.py --mode offline` 只检查协议帧、telemetry 解析、`counts_per_meter` 门禁、launch 契约和串口异常恢复契约，不打开串口、不发布运动命令。
- `stage1_acceptance_check.py --mode live` 只检查已运行 ROS topic 和类型，不发布运动命令。
- `--require-odom` 用于 `/imu/data`、`/wheel_odom` 和 EKF `/odom` 均预期存在的检查场景。
- 工具输出必须回填测试记录；失败项不得人工改成 PASS。

标准非零运动命令只用于架空或低速安全场地：

```bash
timeout 2s ros2 topic pub --rate 10 /ackermann_cmd autoracer_interfaces/msg/AckermannChassisCommand "{header: {frame_id: ''}, speed_mps: 0.10, steering_angle_rad: 0.0, enable: true, brake: false, clear_fault: false, emergency_stop: false}"
timeout 2s ros2 topic pub --rate 10 /ackermann_cmd autoracer_interfaces/msg/AckermannChassisCommand "{header: {frame_id: ''}, speed_mps: 0.10, steering_angle_rad: 0.15, enable: true, brake: false, clear_fault: false, emergency_stop: false}"
timeout 2s ros2 topic pub --rate 10 /ackermann_cmd autoracer_interfaces/msg/AckermannChassisCommand "{header: {frame_id: ''}, speed_mps: 0.10, steering_angle_rad: -0.15, enable: true, brake: false, clear_fault: false, emergency_stop: false}"
ros2 topic pub --once /ackermann_cmd autoracer_interfaces/msg/AckermannChassisCommand "{header: {frame_id: ''}, speed_mps: 0.0, steering_angle_rad: 0.0, enable: true, brake: true, clear_fault: false, emergency_stop: false}"
ros2 topic pub --once /ackermann_cmd autoracer_interfaces/msg/AckermannChassisCommand "{header: {frame_id: ''}, speed_mps: 0.0, steering_angle_rad: 0.0, enable: false, brake: true, clear_fault: false, emergency_stop: true}"
```

记录要求：

- `straight_low_speed`：直行命令，`actual_speed_mps` 和 `/wheel_odom` 方向正确。
- `left_low_speed`：左转命令，`steering_angle_rad` 为正。
- `right_low_speed`：右转命令，`steering_angle_rad` 为负。
- `stop_command`：停车命令，速度回零且 `brake_active` 证据完整。
- `timeout_stop`：停止发布命令后记录 `command_timeout` 和速度回零。
- `estop_command`：上位机急停命令后记录 `estop_active` 和速度回零。

## 阶段 2：路径跟踪

### 当前状态

阶段 2 固定目标入口、test path publisher、point-goal path publisher、Pure Pursuit tracker 和 diagnostics publisher 已实现。

阶段 2 验收 case 固定为 `straight_2m`、`left_arc_r2m`、`right_arc_r2m`、`s_curve`、`stop_at_end`，并扩展 `goal_2m_2m` 用于验证无避障点目标跟踪。未实现对应 fixture、tracker、诊断或上机证据时，测试记录必须标为 `未实现`、`FAIL` 或 `BLOCKED`，不得记为通过。

阶段 2 实现归属：

- 新建 `autoracer_path_tracking` 包承载 test path publisher、Pure Pursuit tracker；tracker 同步发布 diagnostics。
- `autoracer_path_tracking/goal_path_publisher` 负责把当前 `/odom` 位姿到 `goal_x_m/goal_y_m` 的直线生成为 `/path_tracking/path`。
- 新建 `autoracer_bringup/stage2_path_tracking.launch.py` 作为固定目标入口。
- 新建 `autoracer_bringup/stage2_goal_tracking.launch.py` 作为 odom 点目标入口。
- `stage2_path_tracking.launch.py` include 阶段 1 Ackermann 底盘链路，并启动 `autoracer_path_tracking` 中的阶段 2 节点。
- 阶段 2 tracker 默认启用零速预使能门控：发出非零运动命令前，先持续发布 `speed_mps=0`、`enable=true`，直到 `/chassis_state` 显示 `auto_enabled=true`、`command_timeout=false`、`rc_override_active=false`、`estop_active=false`。
- tracker 算法固定为 Pure Pursuit。

当前已准备阶段 2 path fixture 和 fake-odom tracker 检查：

```bash
python3 tools/acceptance/stage2_path_fixture_check.py
python3 tools/acceptance/stage2_tracker_fake_odom_check.py
```

fixture 位置：

```text
tools/acceptance/fixtures/stage2_paths/
```

这些 fixture 是 `nav_msgs/Path` 语义的标准输入，用于 tracker 离线和实车验收。fixture 静态检查本身不发布
`/ackermann_cmd`，fake-odom 检查只证明 tracker 软件输出满足方向、限幅和到点停车契约，不代表实车阶段 2 PASS。

固定目标入口：

```bash
ros2 launch autoracer_bringup stage2_path_tracking.launch.py counts_per_meter:=<实测值>
```

该命令当前可运行；实车运行前必须满足阶段 1 安全链路、`counts_per_meter` 标定和低速可接管条件。
启动后 tracker 会先执行零速预使能。若底盘停留在空闲 RC passthrough 或 command timeout 状态，
tracker 必须保持零速，不得直接释放非零路径命令。

点目标入口示例，不带避障，只验证从当前 `/odom` 位姿到目标点的路径跟踪。当前 `max_steering_angle_rad=0.262` 时，`(2,2)` 对前进-only Ackermann 跟踪过紧；实车验收目标使用 `(3,2)`：

```bash
ros2 launch autoracer_bringup stage2_goal_tracking.launch.py counts_per_meter:=13.545 goal_x_m:=3.0 goal_y_m:=2.0 target_speed_mps:=0.18
```

现场明确批准更高速度时，可以同时覆盖 tracker 的速度上限，例如 `target_speed_mps:=1.00 max_target_speed_mps:=1.00`。

点目标入口默认 `lock_start_pose=true`，即用首次有效 `/odom` 生成到目标点的路径后保持固定，不随车辆运动反复重规划。若前视目标落到车后方且 `allow_reverse=false`，tracker 必须输出停车/刹车命令，不得继续发布前进速度。

### 启动节点

实现完成后必须启动：

| 节点 | 作用 |
| --- | --- |
| 阶段 1 底盘 Ackermann bringup | 提供 `/odom`、`/chassis_state`、`/wheel_odom` 和 `/ackermann_cmd` 串口桥 |
| `autoracer_path_tracking/test_path_publisher` | 读取 fixture，发布 `straight_2m`、`left_arc_r2m`、`right_arc_r2m`、`s_curve`、`stop_at_end` 测试路径 |
| `autoracer_path_tracking/goal_path_publisher` | 读取起始 `/odom`，发布到 `goal_x_m/goal_y_m` 的无避障固定直线路径 |
| `autoracer_path_tracking/pure_pursuit_tracker` | 订阅 `/path_tracking/path` 和 `/odom`，发布 `/ackermann_cmd` 和 `/path_tracking/diagnostics` |

### Topic

必须订阅：

| Topic | 用途 |
| --- | --- |
| `/odom` | 当前位姿输入 |
| `/path_tracking/path` | 阶段 2 测试路径输入，类型 `nav_msgs/msg/Path`，frame `odom` |

必须发布：

| Topic | 用途 |
| --- | --- |
| `/ackermann_cmd` | tracker 输出的底盘命令 |
| `/path_tracking/diagnostics` | 阶段 2 验收记录输入，类型 `autoracer_interfaces/msg/PathTrackingDiagnostics` |

`PathTrackingDiagnostics` 必须包含：

```text
std_msgs/Header header
string case_name
uint32 target_index
float32 lookahead_m
float32 lateral_error_m
float32 heading_error_rad
float32 target_speed_mps
float32 actual_speed_mps
float32 steering_command_rad
bool steering_limited
bool speed_limited
bool stop_commanded
float32 remaining_distance_m
```

### 参数表

固定目标参数：

| 参数 | 验收约束 |
| --- | --- |
| `counts_per_meter` | 必须大于 `0` |
| `target_speed_mps` | 必须使用低速测试值 |
| `max_target_speed_mps` | 默认 `0.25`；只在现场明确批准的实车测试中覆盖，最高不得超过 `1.00` |
| `wheelbase_m` | 必须与阶段 1 车辆参数一致 |
| `max_steering_angle_rad` | 必须与阶段 1 车辆参数一致 |
| `lookahead_m` | 默认 `0.60`，必须写入测试记录 |
| `goal_tolerance_m` | 默认 `0.05`；控制停车窗口。`straight_2m` 实车验收按 `0.10 m` 窗口，要求前进距离不低于 `1.90 m` |
| `control_rate_hz` | 默认 `20` |
| `allow_reverse` | 固定 `false` |
| `prearm_zero_before_motion` | 阶段 2 launch 默认 `true`；非零运动前零速预使能 |
| `prearm_timeout_s` | 默认 `3.0`；超过后只告警，仍保持零速直到底盘自动控制就绪 |
| `lock_start_pose` | 点目标入口默认 `true`；首次生成路径后固定起点，避免运动中把路径改成从当前点回追目标 |

阶段 2 tracker 的 `0.25 m/s` 是默认低速软件上限；经现场批准覆盖到更高速度时，
仍受 STM32 前进命令上限 `1.00 m/s` 约束。霍尔超速保护 `5.0/4.5 m/s`
只用于异常保护，不得写作阶段 2 目标速度。

### 启动前检查

- 阶段 1 `/odom` 已通过验收。
- 阶段 2 实车 launch 不要求人工先单独发零速命令；tracker 内置预使能门控必须能把底盘从空闲安全态带到 `auto_enabled=true` 后才释放非零命令。
- RC 接管、急停、刹车和通信超时停车已通过测试。
- 测试场地或架空条件满足运动测试要求。

### 启动后检查

- tracker 发布 `/ackermann_cmd`。
- test path publisher 发布 `/path_tracking/path`。
- tracker diagnostics 发布 `/path_tracking/diagnostics`。
- `/chassis_state` 的实际速度和转角跟随命令变化。
- 测试记录包含 `straight_2m`、`left_arc_r2m`、`right_arc_r2m`、`s_curve`、`stop_at_end`。
- 测试记录包含横向误差、航向误差、速度变化和转角限幅。
- 测试记录回填根目录阶段验收追踪表。

fake odom 自动检查固定入口：

```bash
python3 tools/acceptance/stage2_tracker_fake_odom_check.py
```

该工具为离线 fake-odom 检查，覆盖：

- 每个 fixture 按 pose 顺序生成 fake `/odom`。
- `straight_2m` 和 `stop_at_end` 的 `abs(steering_angle_rad)` 不超过 fixture `expected.max_abs_steering_rad`。
- `left_arc_r2m` 输出正转角；`right_arc_r2m` 输出负转角。
- `s_curve` 输出先正后负的转角符号序列。
- 非终点段 `0 < speed_mps <= target_speed_mps + 0.02`。
- 全程不输出倒车命令。
- 终点距离进入 `stop_distance_m` 或默认 `0.05 m` 后输出 `speed_mps=0.0` 且 `brake=true`；实车验收允许使用 `acceptance_distance_m=0.10 m` 作为 PASS 窗口。

## 阶段 3：2D SLAM 建图

### 当前状态

固定入口 `autoracer_bringup/stage3_mapping.launch.py` 已实现；下面的分终端命令仍保留为排障和手动展开参考。
上车验收时可以使用固定入口，也可以按分终端方式启动后记录等价证据。

阶段 3 验收 case 固定为 `lidar_topic`、`tf_chain`、`slam_map_live`、`map_save_reload`、`mapping_bag_replay`。未实现 bag replay、地图保存/重载或现场地图质量证据时，测试记录必须标为 `未实现`、`FAIL` 或 `BLOCKED`，不得记为通过。
阶段 3 主线固定为 2D `slam_toolbox` 建图；3D/LIO-SAM 不进入阶段 3 验收主线。
阶段 3 复用 `slam_toolbox`、`pointcloud_to_laserscan`、Nav2 map saver/map server 和阶段 1 底盘/odom 链路；
不启动完整 Nav2 navigation 作为建图主线。扫图运动保留两条路径：RC/manual 低速人工扫第一张地图，
以及已通过阶段 2 验收的 tracker 或后续专用扫图 follower 做自动扫图能力验证。RC/manual 扫图时，
STM32 用 RC throttle 相对中位的正负偏移给霍尔增量赋方向；自动扫图时，方向来自 `/ackermann_cmd.speed_mps`。
同一测试中只能有一个 `/ackermann_cmd` 发布者。

阶段 3 状态分层记录：

- `阶段 3A` 是可用地图产出闭环，包含 `lidar_topic`、`tf_chain`、`slam_map_live`、`map_save_reload`
  和 `mapping_bag_replay`。
- `阶段 3B` 是自动/半自动扫图能力验证，记录
  `/ackermann_cmd -> hall_delta_count -> /wheel_odom -> /odom`；它用于后续自动扫图优化，不替代最终
  RC/manual 一圈闭环地图。

### 专业人员固定执行单

阶段 3 正式启动优先使用固定入口。该入口默认启动：

- `hipnuc_imu` raw IMU driver。
- `imu_filter_madgwick`，发布 `/imu/data`。
- URDF/TF，发布 `base_footprint -> base_link -> laser`。
- 阶段 1 Ackermann chassis bridge、`/wheel_odom` 和 EKF `/odom`。
- LiDAR driver、`pointcloud_to_laserscan` 和 `slam_toolbox`。

启动命令：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
python3 tools/acceptance/stage3_mapping_contract_check.py
ros2 launch autoracer_bringup stage3_mapping.launch.py \
  counts_per_meter:=13.545 \
  use_rviz:=true \
  slam_params_file:=$(ros2 pkg prefix autoracer_slam_toolbox)/share/autoracer_slam_toolbox/config/mapper_params_floor2_dense.yaml
```

如果现场重新标定了霍尔里程计，只能把 `13.545` 替换为新的实测
`counts_per_meter`，并必须把标定记录写入测试记录。

另开一个终端启动证据记录：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 bag record -o docs/test-records/rosbags/stage3-slam-live-$(date +%Y%m%d-%H%M%S) \
  /chassis_state /wheel_odom /odom /imu/data /scan /map /map_metadata /tf /tf_static
```

正式 2D 建图默认不录 `/point_cloud_raw`。实时建图仍然启动 LiDAR 驱动并由
`/point_cloud_raw -> pointcloud_to_laserscan -> /scan` 生成 2D scan；不录 raw cloud
只是为了降低 CPU、磁盘和 RViz 同时运行时的负载。若本次试验目标包含后续 3D SLAM、NDT/LIO-SAM
排障或点云外参分析，必须另开一份原始点云 bag，名称后缀建议为 `-raw-cloud`，至少记录
`/point_cloud_raw /imu/data /odom /wheel_odom /tf /tf_static`。

开车扫图前，专业人员必须原地确认：

```bash
ros2 topic hz /scan
ros2 topic hz /wheel_odom
ros2 topic hz /odom
ros2 run tf2_ros tf2_echo odom laser
ros2 topic echo --qos-durability transient_local /map_metadata --once
ros2 topic echo /chassis_state --once
```

通过判据：

- `/scan` 约 `20 Hz`。
- `/wheel_odom` 约 `20 Hz`。
- `/odom` 约 `30 Hz`。
- `odom -> laser` TF 连通。
- `/map_metadata` 能读到，分辨率为 `0.05 m/pixel`。
- 开车前 `/chassis_state` 速度为 `0`；任何自动控制运动必须另行说明并获得驾驶员许可。

只有上述原地检查全部通过，专业人员才可以请驾驶员确认周围环境并申请开车扫图许可。
驾驶员未明确许可前，不得发布会导致车辆运动的命令。

RC/manual 扫图开始后，专业人员必须先做短距离里程计门禁：低速移动约 `0.5-1.0 m` 后停车，
读取 `/chassis_state` 和 `/wheel_odom`，确认 `hall_delta_count` 在运动窗口内出现非零累计且
`/wheel_odom` 位移随之增加。若 `actual_speed_mps` 非零但 `hall_delta_count` 仍全为 `0`，
必须立即停止本轮扫图并记录为 `BLOCKED`；不得继续保存地图或宣称 `/odom` 可用于 SLAM。

自动扫图验证必须在人工许可后单独执行，并记录 `/ackermann_cmd -> hall_delta_count -> /wheel_odom -> /odom`
闭环证据；它用于证明车辆具备自主扫图能力，不替代第一张地图的 RC/manual 低速建图入口。

扫图结束后保存地图：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch autoracer_robot_nav2 save_map.launch.py \
  output_dir:=artifacts/maps/stage3-$(date +%Y%m%d-%H%M%S) \
  map_name:=autoracer_stage3
```

下面的分终端命令只保留为排障展开方式，不作为首选启动流程。

终端 1，启动 IMU raw 数据：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch hipnuc_imu imu_spec_msg.launch.py
```

终端 2，启动 Madgwick，生成 `/imu/data`：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 run imu_filter_madgwick imu_filter_madgwick_node --ros-args --params-file "$(ros2 pkg prefix turn_on_autoracer_robot)/share/turn_on_autoracer_robot/config/imu.yaml"
```

终端 3，启动 Ackermann 底盘桥和 EKF：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch turn_on_autoracer_robot ackermann_chassis.launch.py counts_per_meter:=<实测值> use_ekf:=true
```

终端 4，启动 LiDAR：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch lslidar_driver lslidar_cx_launch.py
```

终端 5，启动 URDF/TF，提供 `base_link -> laser` 固定传感器 TF：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch autoracer_robot_urdf robot_description.launch.py
```

终端 6，启动 SLAM：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch autoracer_slam_toolbox slam.launch.py include_bringup:=false
```

固定目标入口：

```bash
ros2 launch autoracer_bringup stage3_mapping.launch.py counts_per_meter:=<实测值>
```

固定目标入口已实现。离线契约检查：

```bash
python3 tools/acceptance/stage3_mapping_contract_check.py
```

### 启动节点

固定入口 `stage3_mapping.launch.py` 默认先启动 IMU raw、Madgwick 和
`autoracer_robot_urdf/robot_description.launch.py`，再启动阶段 1、LiDAR 和
`autoracer_slam_toolbox/slam.launch.py include_bringup:=false`。当前
`autoracer_slam_toolbox/slam.launch.py include_bringup:=false` 自身启动：

| 节点 | 作用 |
| --- | --- |
| `pointcloud_to_laserscan/pointcloud_to_laserscan_node`，节点名 `pointcloud_to_laserscan` | `/point_cloud_raw -> /scan` |
| `slam_toolbox/sync_slam_toolbox_node`，节点名 `slam_toolbox` | `/scan + /odom + /tf -> /map` |
| `rviz2`，节点名 `rviz2_slam` | `use_rviz:=true` 时启动 |

`include_bringup:=true` 会 include legacy `turn_on_autoracer_robot.launch.py`。该模式禁止用于阶段 3 验收。

### Topic

必须已有：

| Topic | 来源 |
| --- | --- |
| `/odom` | 阶段 1 canonical odom |
| `/tf`、`/tf_static` | 阶段 1 和 URDF/TF 链路；必须连通 `odom -> base_footprint -> base_link -> laser` |
| `/point_cloud_raw` | LiDAR 驱动 |

必须发布：

| Topic | 来源 |
| --- | --- |
| `/scan` | `pointcloud_to_laserscan` |
| `/map` | `slam_toolbox` |

### 参数表

`slam.launch.py` 参数：

| 参数 | 当前默认值 | 验收约束 |
| --- | --- | --- |
| `include_bringup` | `true` | 阶段 3 验收必须为 `false`，底盘链路单独按阶段 1 启动 |
| `use_rviz` | `true` | 按测试需要设置 |
| `odom_frame` | `odom` | 阶段 3 验收必须使用 `odom` |

`stage3_mapping.launch.py` 额外参数：

| 参数 | 当前默认值 | 验收约束 |
| --- | --- | --- |
| `counts_per_meter` | 无默认可用值 | `start_chassis:=true` 时 launch 会硬性要求大于 `0`；必须使用阶段 1 实测值 |
| `start_imu` | `true` | 正式阶段 3 验收默认启动 IMU raw 和 Madgwick；只有外部已启动等价 `/imu/data` 时可设为 `false` |
| `start_robot_description` | `true` | 正式阶段 3 验收必须启动，除非已有等价 URDF/TF 节点且测试记录写明 |
| `start_chassis` | `true` | 正式阶段 3 验收必须启动阶段 1 底盘链路 |
| `start_lidar` | `true` | 正式阶段 3 验收必须启动 LiDAR 输入 |
| `slam_params_file` | `autoracer_slam_toolbox/config/mapper_params_online_sync.yaml` | 可切换 SLAM 参数；正式二楼环形通道地图建议使用 `mapper_params_floor2_dense.yaml` |

阶段 3 开车扫图前必须先完成原地检查：

```bash
ros2 run tf2_ros tf2_echo odom base_link
ros2 topic hz /wheel_odom
ros2 topic hz /odom
ros2 topic hz /point_cloud_raw
ros2 topic hz /scan
ros2 topic echo --qos-durability transient_local /map_metadata --once
```

只有 `odom -> base_link` 连通、`/wheel_odom` 和 `/odom` 有稳定频率、`/scan` 有稳定频率、
`/map_metadata` 能读到时，才允许进入人工许可后的开车扫图。缺任意一项时，只做原地排障。

阶段 3 `pointcloud_to_laserscan` 固定参数基线：

| 参数 | 默认值 | 验收约束 |
| --- | --- | --- |
| `target_frame` | `base_link` | TF 必须能从 `base_link` 查询到 LiDAR frame；若实车 TF 改为 `base_footprint`，必须同步 launch 和记录 |
| `transform_tolerance` | `0.01` | TF 延迟超出时不得 PASS |
| `min_height` | `0.1` | 过滤低于该高度的点；改动必须记录现场原因 |
| `max_height` | `1.5` | 高于该高度的点不进入 2D scan |
| `angle_min` | `-3.14159` | 默认全向 scan |
| `angle_max` | `3.14159` | 默认全向 scan |
| `angle_increment` | `0.00436` | 约 `0.25°` |
| `scan_time` | `0.05` | 目标 `20 Hz` |
| `range_min` | `0.2` | 近距离盲区内的点不作为建图证据 |
| `range_max` | `20.0` | 室内阶段 3 默认上限 |
| `use_inf` | `true` | 无返回值用 `inf` 表示 |
| `inf_epsilon` | `1.0` | 与 launch 当前值一致 |

阶段 3 `slam_toolbox` 固定参数基线：

| 参数 | 默认值 | 验收约束 |
| --- | --- | --- |
| `mode` | `mapping` | 阶段 3 必须为建图模式 |
| `map_frame` | `map` | 输出地图 frame |
| `odom_frame` | `odom` | 使用 phase-1 canonical `/odom` |
| `base_frame` | `base_footprint` | 与车辆 TF 语义一致 |
| `scan_topic` | `/scan` | 只能使用阶段 3 转换后的 2D scan |
| `map_update_interval` | `5.0` | 地图更新频率改变需记录 |
| `resolution` | `0.05` | 地图分辨率，单位 m/pixel |
| `max_laser_range` | `20.0` | 必须与 scan `range_max` 协调 |
| `minimum_time_interval` | `0.5` | 扫描处理最小时间间隔 |
| `transform_timeout` | `0.2` | TF 超时不得静默忽略 |
| `minimum_travel_distance` | `0.5` | 新扫描触发位移阈值 |
| `minimum_travel_heading` | `0.5` | 新扫描触发角度阈值 |
| `do_loop_closing` | `true` | 关闭回环必须写入验收记录 |

二楼环形通道、开阔区域或后续阶段 4 目标地图建议使用 dense 2D profile：

```bash
ros2 launch autoracer_bringup stage3_mapping.launch.py \
  counts_per_meter:=13.545 \
  use_rviz:=true \
  slam_params_file:=$(ros2 pkg prefix autoracer_slam_toolbox)/share/autoracer_slam_toolbox/config/mapper_params_floor2_dense.yaml
```

`mapper_params_floor2_dense.yaml` 保持 `resolution=0.05`，并把 scan 插入阈值调为
`minimum_time_interval=0.20`、`minimum_travel_distance=0.18`、`minimum_travel_heading=0.12`，
`throttle_scans=2`、`scan_buffer_size=20`。这个配置目标是在开发笔记本实时负载内保留比默认值更密的 scan 插入；它仍是 2D
`slam_toolbox` scan matching，不是 3D 点云匹配。

### 启动前检查

```bash
ros2 topic hz /odom
ros2 topic hz /point_cloud_raw
ros2 run tf2_ros tf2_echo base_link laser
```

必须确认：

- `/odom` 来自 phase-1 Ackermann 链路。
- LiDAR frame 和车体 TF 存在。
- 地图保存路径已确定。

### 启动后检查

```bash
ros2 topic hz /scan
ros2 topic echo /map --once
ros2 run tf2_ros tf2_echo map odom
```

必须检查：

- `/scan` 发布频率稳定。
- `/map` 有数据。
- `map -> odom` 存在。
- rosbag 记录 `/ackermann_cmd`、`/chassis_state`、`/wheel_odom`、`/odom`、`/imu/data`、`/scan`、`/map`、`/map_metadata`、`/tf`、`/tf_static`。
- 正式 2D 建图 bag 不要求记录 `/point_cloud_raw`；3D 阶段或点云排障需要单独记录 raw cloud bag。
- 地图保存、重载和 rosbag replay 结果回填根目录阶段验收追踪表；保存证据至少包含 `map.yaml`、地图图像、保存命令、重载命令、SLAM 日志和 rosbag 路径。

阶段 3 当前地图保存命令：

```bash
ros2 launch autoracer_robot_nav2 save_map.launch.py \
  map_name:=<map_name> \
  output_dir:=artifacts/<run-id>/maps
```

`output_dir` 必须是测试记录中的正式证据目录；默认值 `artifacts/maps` 只用于临时排障。
保存后至少应得到 `<map_name>.yaml` 和对应地图图像文件，并把完整路径写入阶段 3 记录。

阶段 3 记录中的重载命令必须使用后续阶段 4 相同的 map server/Nav2 地图加载路径，例如：

```bash
ros2 launch autoracer_robot_nav2 navigation.launch.py map:=artifacts/<run-id>/maps/<map_name>.yaml
```

该重载命令只用于证明地图文件可加载，不等价于阶段 4 导航 PASS。

## 阶段 4：2D Nav2 阿克曼地图导航

### 当前状态

固定入口 `autoracer_bringup/stage4_navigation.launch.py` 已实现，默认使用
`autoracer_robot_nav2/param/stage4_nav2_params.yaml`。下面的 `navigation.launch.py` 是过渡入口；
包内 `autoracer_nav2_params.yaml` 仍是 MPPI 旧配置，只能作为迁移对照。
稳定版阶段 4 验收目标是 Nav2 官方阿克曼链路：
AMCL、costmap、Smac Hybrid-A*、Regulated Pure Pursuit、Collision Monitor 和
`twist_to_ackermann` adapter。当前配置中已有 MPPI 痕迹，MPPI 不作为阶段 4 稳定版主线。
阶段 4 第一版采用复用优先策略：复用 Nav2 map server、AMCL、costmap、Smac Hybrid-A*、RPP 和
Collision Monitor；项目自有部分只做 `/safe_nav2_cmd_vel -> /ackermann_cmd` 的 adapter、topic graph
隔离、diagnostics 和验收检查。后续若要替换为自研 Ackermann tracker，必须先用 rosbag 和 diagnostics
证明 RPP/adapter 是主要误差来源，且不得降低 Collision Monitor、限幅、停车和 `/cmd_vel` 隔离证据要求。

终端 1，启动 IMU raw 数据：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch hipnuc_imu imu_spec_msg.launch.py
```

终端 2，启动 Madgwick，生成 `/imu/data`：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 run imu_filter_madgwick imu_filter_madgwick_node --ros-args --params-file "$(ros2 pkg prefix turn_on_autoracer_robot)/share/turn_on_autoracer_robot/config/imu.yaml"
```

终端 3，启动 Ackermann 底盘桥和 EKF：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch turn_on_autoracer_robot ackermann_chassis.launch.py counts_per_meter:=<实测值> use_ekf:=true
```

终端 4，启动 LiDAR：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch lslidar_driver lslidar_cx_launch.py
```

终端 5，启动 Nav2 过渡入口：

```bash
cd /home/car/CodeWisdom-AutoRacer
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch autoracer_robot_nav2 navigation.launch.py map:=docs/test-records/maps/stage3-final-floor2-loop-20260517-101119.yaml
```

固定目标入口：

```bash
ros2 launch autoracer_bringup stage4_navigation.launch.py map:=docs/test-records/maps/stage3-final-floor2-loop-20260517-101119.yaml counts_per_meter:=<实测值>
```

固定目标入口已实现，默认参数文件为
`autoracer_robot_nav2/param/stage4_nav2_params.yaml`。离线契约检查：

```bash
python3 tools/acceptance/stage4_navigation_contract_check.py
```

### 启动节点

固定入口 `stage4_navigation.launch.py` 默认启动：

| 节点或 include | 作用 |
| --- | --- |
| `hipnuc_imu/imu_spec_msg.launch.py` | 发布 `/imu/data_raw` |
| `imu_filter_madgwick/imu_filter_madgwick_node` | `/imu/data_raw -> /imu/data` |
| `autoracer_robot_urdf/robot_description.launch.py` | 发布 URDF/TF，提供 `base_link -> laser` 固定传感器 TF |
| `autoracer_bringup/stage1_chassis.launch.py` | 启动阶段 1 底盘、wheel odom 和 EKF 链路 |
| `lslidar_driver/lslidar_cx_launch.py` | 发布 `/point_cloud_raw` LiDAR 输入 |
| `pointcloud_to_laserscan/pointcloud_to_laserscan_node`，节点名 `pointcloud_to_laserscan` | `/point_cloud_raw -> /scan` |
| `rclcpp_components/component_container_isolated`，节点名 `nav2_container` | 仅显式 `use_composition:=True` 时作为 Nav2 container；阶段 4 实车默认使用非组合节点，便于 lifecycle、topic 和 action server 验收 |
| `nav2_bringup/launch/localization_launch.py` | 加载地图和 AMCL |
| `nav2_bringup/launch/navigation_launch.py` | 启动 Nav2 行为树、controller、planner 等导航节点 |
| Smac Hybrid-A* | 使用真实 footprint 和最小转弯半径生成阿克曼可行 path |
| Regulated Pure Pursuit | 跟踪 Nav2 path，输出内部 `cmd_vel_nav` |
| Velocity Smoother | `cmd_vel_nav -> /nav2_cmd_vel`，执行 `0.40 m/s` 前进上限、`0.18 rad/s` yaw 上限和加减速度约束 |
| Collision Monitor | 对 `/nav2_cmd_vel` 执行 stop/slowdown，输出 `/safe_nav2_cmd_vel` |
| `twist_to_ackermann` adapter | `/safe_nav2_cmd_vel -> /ackermann_cmd`，处理限幅、倒车和不可行原地旋转请求 |
| topic graph 检查 | 证明 Nav2 `/cmd_vel` 没有直接进入 `ackermann_chassis_bridge` 或 STM32 |
| `rviz2`，节点名 `rviz2_stage4_nav` | `use_rviz:=true` 时启动 |

### Topic

必须已有：

| Topic | 来源 |
| --- | --- |
| `/odom` | 阶段 1 canonical odom |
| `/tf`、`/tf_static` | 阶段 1、定位和 URDF/TF 链路；必须连通 `map/odom -> base_footprint -> base_link -> laser` |
| `/point_cloud_raw` | LiDAR 驱动 |

必须发布或使用：

| Topic | 用途 |
| --- | --- |
| `/scan` | costmap 和定位输入 |
| `/map` | 地图服务或定位输出 |
| `cmd_vel_nav` | Nav2 controller 到 velocity smoother 的内部 Twist |
| `/nav2_cmd_vel` | velocity smoother 输出的 Nav2 内部 Twist；禁止直接进入 STM32 |
| `/safe_nav2_cmd_vel` | Collision Monitor 过滤后的内部安全 Twist |
| `/ackermann_cmd` | 最终底盘命令输出目标 |
| `/twist_to_ackermann/diagnostics` | `diagnostic_msgs/msg/DiagnosticArray`；adapter 输入、输出、限幅、倒车、不可行请求和停车原因 |

### 参数表

稳定版固定入口 `stage4_navigation.launch.py` 参数：

| 参数 | 当前默认值 | 验收约束 |
| --- | --- | --- |
| `map` | 无默认可用值 | `start_nav2:=true` 时 launch 会硬性要求显式传入存在的 `.yaml` 地图；阶段 4 验收必须使用阶段 3 实测保存地图 |
| `params_file` | `autoracer_robot_nav2/param/stage4_nav2_params.yaml` | 4A 默认 forward-only 参数；4B 候选使用 `stage4_nav2_reverse_params.yaml`，必须在记录中显式写明 |
| `use_sim_time` | `false` | 实车使用 `false` |
| `autostart` | `true` | 按测试记录写明 |
| `use_composition` | `False` | 阶段 4 实车默认使用非组合 Nav2，便于 lifecycle、topic 和 action server 验收；如显式改为 `True`，测试记录必须写明 |
| `use_respawn` | `False` | 按测试记录写明 |
| `use_rviz` | `true` | 按测试需要设置 |
| `log_level` | `info` | 调试时可改，测试记录必须写明 |
| `counts_per_meter` | 无默认可用值 | `start_chassis:=true` 时 launch 会硬性要求大于 `0`；阶段 4 验收必须使用已通过阶段 1 的实测值 |
| `start_imu` | `true` | 正式阶段 4 验收默认启动 IMU raw 和 Madgwick；只有外部已启动等价 `/imu/data` 时可设为 `false` |
| `start_robot_description` | `true` | 正式阶段 4 验收必须启动，除非已有等价 URDF/TF 节点且测试记录写明 |
| `start_chassis` | `true` | 正式阶段 4 验收必须启动阶段 1 底盘链路 |
| `start_lidar` | `true` | 正式阶段 4 验收必须启动 LiDAR 输入 |
| `start_nav2` | `true` | 仅本地 launch 解析排障时可设为 `false`；验收必须为 `true` |
| `wheelbase_m` | `0.60` | 必须与实车轴距标定一致 |
| `max_steering_angle_rad` | `0.262` | 必须与阶段 1 下位机转角限幅一致 |
| `allow_reverse` | `false` | 阶段 4A 必须为 `false`；4B 候选必须显式覆盖为 `true` 并使用 reverse 参数文件 |

稳定版阶段 4 参数约束：

| 项 | 验收约束 |
| --- | --- |
| planner | 使用 `nav2_smac_planner/SmacPlannerHybrid` |
| controller | 使用 `nav2_regulated_pure_pursuit_controller/RegulatedPurePursuitController` 作为阶段 4 主线 |
| 目标速度 | RPP `desired_linear_vel=0.40 m/s`，velocity smoother 前进上限 `0.40 m/s`、yaw 上限 `0.18 rad/s`；adapter 自动前进硬限幅仍为 `1.00 m/s` |
| 接近/曲率限速最低速度 | RPP `min_approach_linear_velocity=0.35 m/s`、`regulated_linear_scaling_min_speed=0.35 m/s`，避免进入本车实测低速阻力推不动区 |
| progress checker | `required_movement_radius=0.25 m`、`movement_time_allowance=20.0 s`，给 STM32 低速/满舵 PID 起扭矩留出时间，但长期无位移仍 abort |
| 到点容差 | SimpleGoalChecker `xy_goal_tolerance=0.35 m`、`yaw_goal_tolerance=0.35 rad`；阶段 4A 记录必须写明该容差 |
| behavior tree | `NavigateToPose` 使用 `stage4_navigate_to_pose_goal_update_only.xml`，只在目标更新时重规划；`NavigateThroughPoses` 使用 `stage4_navigate_through_poses_no_recovery.xml`；不得加载 `Spin`、`BackUp` 或 `DriveOnHeading` |
| `minimum_turning_radius` | 使用实车标定值；当前文档事实为 `2.24 m` |
| 4A 倒车 | Smac `motion_model_for_search=DUBIN` 或等价无倒车 cusp 配置；RPP `allow_reversing=false` |
| 4B 倒车 | 候选参数文件 `stage4_nav2_reverse_params.yaml` 使用 `REEDS_SHEPP`、RPP `allow_reversing=true` 和倒车 `vx >= -0.35 m/s`；仅在后方/侧方安全区和倒车限速证据齐全时上车开启 |
| 原地转向 | 必须禁用；例如 RPP `use_rotate_to_heading=false` |
| Nav2 Twist | controller 输出隔离为 `cmd_vel_nav`，velocity smoother 输出隔离为 `/nav2_cmd_vel`，Collision Monitor 输出隔离为 `/safe_nav2_cmd_vel`，不得直接发到底盘 |
| adapter diagnostics | 固定为 `/twist_to_ackermann/diagnostics`，类型 `diagnostic_msgs/msg/DiagnosticArray` |

阶段 4 默认完成范围：

- 阶段 4A 是稳定版阶段 4 的默认 PASS 范围；除 `reverse_plan_controlled` 外，阶段 4 验收 case 必须有 PASS 或明确的安全 `BLOCKED` 证据。
- `reverse_plan_controlled` 是阶段 4B 扩展项；阶段 4A 通过时可记录为 `未实现/4B 后续`，不得伪记为 PASS。
- 阶段 4B 只有在倒车路径、倒车限速、后方/侧方 Collision Monitor 安全区和人工安全确认都齐全时才能 PASS。

阶段 4 算法和转换边界：

- Smac Hybrid-A* 负责生成满足 footprint 和最小转弯半径的 path。
- RPP 负责跟踪 path 并输出内部 `vx/wz`，不直接作为 STM32 控制接口。
- `twist_to_ackermann` 使用低速 bicycle model：`steering_angle_rad = atan(wheelbase_m * wz / vx)`。
- `vx≈0 && wz!=0` 属于阿克曼不可行原地旋转请求，adapter 必须停车或拒绝。
- adapter 必须对速度、转角、倒车、NaN/inf 和超时做限幅或停车处理，并通过 diagnostics 记录原因。

adapter 默认参数和判定：

| 参数 | 默认值 | 验收约束 |
| --- | --- | --- |
| `wheelbase_m` | `0.60` | 与阶段 1 车辆参数一致 |
| `max_steering_angle_rad` | `0.262` | 与阶段 1 转角限幅一致 |
| `max_auto_speed_mps` | `1.00` | 自动前进命令限幅；霍尔超速保护不是导航速度目标 |
| `max_reverse_speed_mps` | `0.60` | 仅阶段 4B 使用 |
| `min_forward_drive_speed_mps` | `0.35` | Collision Monitor 或 smoother 输出低于该值的非零前进命令时停车，不向 STM32 透传爬行速度 |
| `min_reverse_drive_speed_mps` | `0.35` | 4B 倒车候选同样不透传爬行倒车速度 |
| `min_turn_speed_mps` | `0.05` | 低于该 `abs(vx)` 不计算 `atan(wheelbase*wz/vx)` |
| `angular_deadband_radps` | `0.02` | 小于等于该 `abs(wz)` 视为零角速度 |
| `allow_reverse` | `false` | 阶段 4A 必须为 `false` |
| `input_timeout_sec` | `0.50` | 超时后 adapter 以停车输出维持安全 |
| `brake_on_stop` | `true` | 主动停车时 `brake=true` |
| `enable_on_command` | `true` | 正常命令输出时 `enable=true` |
| `publish_timeout_stop` | `true` | 输入超时时持续发布停车命令 |
| `diagnostics_rate_hz` | `10.0` | diagnostics 发布频率 |

| 输入/状态 | 输出动作 | diagnostics |
| --- | --- | --- |
| `vx=0` 且 `abs(wz)<=angular_deadband_radps` | 停车输出 | `stop_reason=zero_command` |
| `abs(vx)<min_turn_speed_mps` 且 `abs(wz)>angular_deadband_radps` | 停车输出，不计算转角 | `infeasible_spin=true`、`stop_reason=infeasible_spin` |
| NaN/inf | 停车输出 | `stop_reason=invalid_input` |
| 输入超时 | 持续停车输出直到收到新合法输入 | `stop_reason=input_timeout` |
| `vx<0` 且 `allow_reverse=false` | 停车输出 | `reverse=true`、`stop_reason=reverse_disabled` |
| 速度或转角超限 | 夹紧到允许值 | `speed_limited=true` 或 `steering_limited=true`，`stop_reason=none` |
| 正常可行输入 | 输出 Ackermann 命令 | `stop_reason=none` |

`stop_reason` 枚举固定为 `none`、`zero_command`、`invalid_input`、`input_timeout`、`infeasible_spin`、`reverse_disabled`。
adapter 停车输出固定为 `speed_mps=0.0`、`steering_angle_rad=0.0`、`enable=true`、`brake=brake_on_stop`、`clear_fault=false`、`emergency_stop=false`。
Collision Monitor 的障碍原因由 Collision Monitor 日志、`/safe_nav2_cmd_vel` 和 rosbag 证明，adapter 不凭零 Twist 猜测障碍原因。

Collision Monitor 默认安全区基线：

坐标以 `base_footprint` 为准，`x` 向前、`y` 向左；当前车身 footprint 为
`x=[-0.16, 0.7175]`、`y=[-0.285, 0.285]`。阶段 4A 必须至少实现前向两个 Collision Monitor 区。
ROS2 Humble 不使用后续版本才支持的 Collision Monitor `limit` action，前向绝对限速由 velocity smoother
和 `twist_to_ackermann` 承担：

| Zone | Polygon `[[x,y], ...]` | 动作 | 验收约束 |
| --- | --- | --- | --- |
| `front_stop` | `[[0.72,-0.35],[1.05,-0.35],[1.05,0.35],[0.72,0.35]]` | `stop` | 障碍进入该区时 `/safe_nav2_cmd_vel` 必须为停车或触发安全停车 |
| `front_slowdown` | `[[0.72,-0.45],[1.50,-0.45],[1.50,0.45],[0.72,0.45]]` | `slowdown` | 建议 slowdown ratio `0.35`；必须留下限速证据 |

阶段 4B 受控倒车必须额外实现后方/侧方区，否则 `reverse_plan_controlled` 只能记为 `未实现` 或 `BLOCKED`：

| Zone | Polygon `[[x,y], ...]` | 动作 | 验收约束 |
| --- | --- | --- | --- |
| `rear_stop` | `[[-0.55,-0.35],[-0.16,-0.35],[-0.16,0.35],[-0.55,0.35]]` | `stop` | 倒车障碍进入该区时停车 |
| `rear_slowdown` | `[[-1.20,-0.45],[-0.16,-0.45],[-0.16,0.45],[-1.20,0.45]]` | `slowdown` | 倒车限速证据齐全 |
| `left_side_stop` | `[[-0.16,0.285],[0.72,0.285],[0.72,0.45],[-0.16,0.45]]` | `stop` | 侧向贴近障碍时不允许自动倒车 PASS |
| `right_side_stop` | `[[-0.16,-0.45],[0.72,-0.45],[0.72,-0.285],[-0.16,-0.285]]` | `stop` | 侧向贴近障碍时不允许自动倒车 PASS |

### 启动前检查

```bash
test -f docs/test-records/maps/stage3-final-floor2-loop-20260517-101119.yaml
ros2 topic hz /odom
ros2 topic hz /point_cloud_raw
ros2 run tf2_ros tf2_echo odom base_footprint
ros2 run tf2_ros tf2_echo base_link laser
```

必须确认：

- 地图文件存在。
- 阶段 1 底盘链路已启动。
- LiDAR 已启动。
- RC 接管、急停、刹车和通信超时停车有效。
- Nav2 参数使用 Ackermann 约束。
- `/cmd_vel` 不作为阶段 4 验收入口；Nav2 内部 Twist 链路为 `cmd_vel_nav -> /nav2_cmd_vel -> /safe_nav2_cmd_vel`。
- 稳定版入口已禁用、remap 或隔离 legacy `cmd_vel`，不会让 Nav2 默认 `/cmd_vel` 直连 `ackermann_chassis_bridge`。

### 启动后检查

```bash
ros2 lifecycle nodes
ros2 topic hz /scan
ros2 topic echo /amcl_pose --once
ros2 topic echo /nav2_cmd_vel --once
ros2 topic echo /safe_nav2_cmd_vel --once
ros2 topic echo /twist_to_ackermann/diagnostics --once
ros2 topic echo /ackermann_cmd --once
ros2 node info /ackermann_chassis_bridge
```

必须检查：

- Nav2 lifecycle 节点处于 active。
- `/scan` 必须持续发布；LiDAR `poll() timeout` 或 `/scan` 无数据时停止在静态检查，不得继续跑车。
- RViz 初始位姿设置后定位稳定。
- 目标点测试时 RPP 输出内部 `cmd_vel_nav`，velocity smoother 输出 `/nav2_cmd_vel`，Collision Monitor 输出 `/safe_nav2_cmd_vel`。
- adapter 最终输出 `/ackermann_cmd`，且不可行原地旋转请求被停车或拒绝。
- `ros2 node info` 或 topic graph 证明 `/cmd_vel` 没有直接进入串口桥；如桥仍保留 legacy `cmd_vel` 订阅，阶段 4 记录必须证明 Nav2 没有发布到该 topic。
- 固定入口会把 `ackermann_chassis_bridge` 的 legacy `cmd_vel` 订阅 remap 到 `/stage4_legacy_cmd_vel_disabled`；阶段 4 记录必须证明 `/cmd_vel` 不再和底盘桥连通。
- adapter diagnostics 至少记录 `input_vx_mps`、`input_wz_radps`、`output_speed_mps`、`output_steering_angle_rad`、`speed_limited`、`steering_limited`、`reverse`、`infeasible_spin` 和 `stop_reason`。
- 障碍场景能停车、限速、重规划或明确进入 `BLOCKED`。
- 失败时车辆停车或进入安全状态。

## 禁止用于验收的 legacy 入口

以下入口当前存在，但禁止作为阶段 1 至阶段 4 的正式验收入口：

```bash
ros2 launch turn_on_autoracer_robot turn_on_autoracer_robot.launch.py
ros2 launch autoracer_slam_toolbox slam.launch.py
ros2 launch autoracer_slam_toolbox slam.launch.py odom_frame:=odom_combined
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist ...
```

禁止原因：

- `turn_on_autoracer_robot.launch.py` 使用 legacy/default 语义：`autoracer_robot` 订阅 `/cmd_vel`，发布 raw `/odom`。
- `autoracer_ekf.launch.py` 在 legacy/default 链路中输出 `/odom_combined`。
- 阶段验收要求 canonical `/odom`。
- 阶段验收要求正式底盘命令入口为 `/ackermann_cmd`。
- `slam.launch.py` 默认 `include_bringup:=true` 会拉起 legacy/default bringup。
- 阶段 4 中 `/cmd_vel` 只能作为 Nav2 内部中间控制量的隔离/remap 概念，不得直接作为底盘输入。

允许范围：

- 迁移对照。
- 手动调试。
- 旧链路兼容检查。

## 固定目标入口实现要求

以下入口是阶段固定目标入口。不得删除验收目标，不得把 legacy 入口改写为验收入口。

```bash
ros2 launch autoracer_bringup stage1_chassis.launch.py counts_per_meter:=<实测值> use_ekf:=true
ros2 launch autoracer_bringup stage2_path_tracking.launch.py counts_per_meter:=<实测值>
ros2 launch autoracer_bringup stage3_mapping.launch.py counts_per_meter:=<实测值>
ros2 launch autoracer_bringup stage4_navigation.launch.py map:=docs/test-records/maps/stage3-final-floor2-loop-20260517-101119.yaml counts_per_meter:=<实测值>
```

实现要求：

- `stage1_chassis.launch.py` 必须启动或替代当前 `ackermann_chassis.launch.py` 的底盘桥。
- `stage1_chassis.launch.py` 必须补齐 IMU、URDF/TF 和可选 RViz。
- `stage2_path_tracking.launch.py` 必须复用阶段 1 底盘链路。
- `stage3_mapping.launch.py` 必须使用 phase-1 canonical `/odom`。
- `stage4_navigation.launch.py` 必须使用已保存地图、LiDAR、AMCL、Smac Hybrid-A*、Regulated Pure Pursuit、Collision Monitor、`twist_to_ackermann` 和 `/ackermann_cmd` 输出链路。
- `stage4_navigation.launch.py` 必须把 Nav2 controller 输出隔离为 `cmd_vel_nav`，velocity smoother 输出隔离为 `/nav2_cmd_vel`，Collision Monitor 输出隔离为 `/safe_nav2_cmd_vel`，并提供 `/twist_to_ackermann/diagnostics`。
- 阶段 4A 只验收单目标点 forward-only 导航；多目标点、路线任务、MPPI、复杂动态避障和 3D 感知增强不属于稳定版阶段 4 主线。

## 文档同步

修改 launch、参数、topic 或阶段入口时必须同步检查：

- `CodeWisdom-AutoRacer/README.md`
- `CodeWisdom-AutoRacer/docs/开发流程与验证规范.md`
- 根 `../docs/阶段路线图.md`
- 根 `../docs/系统架构与数据流.md`
- 根 `../docs/接口与协议.md`
- 根 `../docs/开发流程与验证规范.md`
