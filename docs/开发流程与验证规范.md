# 上位机开发流程与验证规范

本文约束 `CodeWisdom-AutoRacer/` 子仓库内的提交时机、提交粒度、构建验证和文档同步。
启动入口、launch 参数和运行检查以 `启动与运行规范.md` 为准。

## 当前分支

当前阶段 1 上位机工作分支：

```text
feature/ackermann-chassis
```

常用定位命令：

```bash
git status --short --branch
git branch --show-current
git branch --list
git remote -v
git log -1 --oneline --decorate
```

## 提交时机

满足以下条件后提交：

- 当前分支已经确认。
- 本次任务相关文件已经完整暂存。
- `msg`、node、launch、config、URDF、TF、odom、串口桥或 Nav2/SLAM 参数变化已经同步到根仓库对应文档。
- 影响阶段 1-4 验收的上位机改动已经按根 `docs/阶段路线图.md` 和 `docs/开发流程与验证规范.md` 的验收契约回填追踪表。
- 本仓库 README、launch/config、接口或流程文档变化后，已经检查根 `README.md`、`AGENTS.md` 和根 `docs/` 是否需要同步。
- 本仓库 README、`启动与运行规范.md` 或本文件已经同步本仓库流程变化。
- 当前机器构建验证已经通过，或文档-only 改动已经通过 `git diff --check`。
- 构建命令、结果和日志路径已经写入测试记录。

## 提交粒度

- message/interface 变化单独提交。
- 串口桥节点变化单独提交。
- launch/config 参数变化单独提交。
- URDF/TF/odom 几何语义变化单独提交。
- SLAM/Nav2 配置变化单独提交。
- 文档-only 修正单独提交。
- 跨下位机协议变化时，本仓库提交只承载上位机消息、桥接、launch/config 和本仓库文档；根仓库最后记录跨仓库文档和子模块指针。

## 本地验证

列出包名：

```bash
colcon list --base-paths src --names-only
```

全量构建：

```bash
colcon build --symlink-install
```

单包构建：

```bash
colcon build --symlink-install --packages-select <package_name>
```

LiDAR 驱动构建依赖 `libpcap-dev`。如果 `lslidar_driver` 构建时报 `pcap.h: No such file or directory`，
先在目标环境安装该系统依赖；没有 sudo 权限的开发机可先用 `--packages-ignore lslidar_driver`
完成不依赖 LiDAR 驱动编译的阶段 3/4 离线契约检查，但不得把这当作完整上车环境构建 PASS。
全量构建还要求可选传感器包依赖齐全，例如 ZED SDK 和旧 gmapping 头文件；开发机缺少这些依赖时，
用 `--packages-select` 验证本轮改动涉及包，并在记录中明确完整构建缺口。

文档和格式检查：

```bash
git diff --check
```

阶段 1 最小验收工具：

```bash
python3 tools/acceptance/stage1_acceptance_check.py --mode offline
python3 tools/acceptance/stage1_acceptance_check.py --mode live
python3 tools/acceptance/stage1_acceptance_check.py --mode live --require-odom
```

`--mode offline` 不打开串口、不发布运动命令；`--mode live` 只检查已运行 topic 和类型。`--require-odom` 用于阶段 1 odom 链路已启动的检查场景。

阶段 2 path fixture 检查：

```bash
python3 tools/acceptance/stage2_path_fixture_check.py
python3 tools/acceptance/stage2_tracker_fake_odom_check.py
```

当前阶段 2 已准备 fixture、Pure Pursuit tracker 和 fake-odom 离线检查。fake-odom 检查通过只代表软件契约满足；缺少
实车 `/odom`、`/ackermann_cmd`、`/path_tracking/diagnostics`、rosbag/log 和安全记录时，阶段 2 仍不得写成实车 PASS。
当前 `max_steering_angle_rad=0.262` 时，`goal_2m_2m` 对前进-only Ackermann 点目标跟踪过紧，只作为失败/几何边界记录；
阶段 2 点目标实车 PASS 使用 `goal_3m_2m` 或显式 `goal_x_m:=3.0 goal_y_m:=2.0`。

阶段 2 实车入口必须启用 tracker 零速预使能门控。非零路径命令释放前，测试记录应能看到 `/chassis_state`
从空闲安全态进入 `auto_enabled=true`、`command_timeout=false`、`rc_override_active=false`；若该状态未满足，
tracker 只能保持 `speed_mps=0` 的预使能命令，不得直接执行路径。

涉及 `/ackermann_cmd`、`/chassis_state`、`/wheel_odom`、`/odom`、TF、IMU、LiDAR、launch/config、Nav2/SLAM 参数或串口桥时，同步核对根仓库 `README.md`、`AGENTS.md`、`docs/阶段路线图.md`、`docs/接口与协议.md`、`docs/系统架构与数据流.md` 和 `docs/开发流程与验证规范.md`。

阶段 1-4 的测试记录必须使用根目录阶段验收契约中的 case 名、状态和证据要求。未实现 fixture、脚本、launch 或硬件条件时，记录为 `未实现`、`FAIL` 或 `BLOCKED`，不得静默跳过或写成 PASS。阶段 4 还必须记录 `/cmd_vel` 隔离证据和 `/twist_to_ackermann/diagnostics`。

阶段 3/4 采用复用优先策略。阶段 3 复用 `slam_toolbox`、`pointcloud_to_laserscan`、Nav2 map saver/map server 和阶段 1 底盘/odom 链路，不启动完整 Nav2 navigation 作为建图主线；RC/manual 低速人工扫图和自动低速/预设路径扫图都保留，任一方式都必须记录 `/chassis_state.hall_delta_count` 非零累计。若复用阶段 2 tracker 执行预设扫图 path，同一测试中只能有一个 `/ackermann_cmd` 发布者。阶段 4 第一版复用 Nav2 map server、AMCL、costmap、Smac Hybrid-A*、RPP 和 Collision Monitor；本仓库自有实现只补 `twist_to_ackermann`、topic graph 隔离、diagnostics 和验收检查。

正式阶段 3 二维建图可使用 `mapper_params_floor2_dense.yaml` 作为 balanced dense 2D profile：地图分辨率保持 `0.05 m/pixel`，scan 插入阈值为 `0.18 m / 0.12 rad / 0.20 s`，并使用 `throttle_scans=2` 控制开发笔记本实时负载。该 profile 仍是 `slam_toolbox` 2D scan matching，不是 NDT、LIO-SAM 或其他 3D 点云匹配。正式 2D 建图 bag 默认不录 `/point_cloud_raw`，以降低 CPU、磁盘和 RViz 同时运行时的负载；若测试目标包含 3D SLAM、NDT/LIO-SAM、点云外参或 raw cloud 复盘，必须另存 `-raw-cloud` bag，至少包含 `/point_cloud_raw /imu/data /odom /wheel_odom /tf /tf_static`。

阶段 4A forward-only 单目标点导航是默认阶段 4 PASS 范围；`reverse_plan_controlled` 属于阶段 4B 扩展项，4A 通过时只能记录为 `未实现/4B 后续`，不得伪记为 PASS。不上车预检只能证明 `stage4_nav2_reverse_params.yaml`、`allow_reverse:=true`、adapter 倒车限幅和后方/侧方 Collision Monitor 区配置存在，不能替代 4B 上车安全确认。
阶段 4A 当前跑车目标速度按 `0.40 m/s` 记录和验收；当前实车参数按 `min_approach_linear_velocity=0.35 m/s`、`regulated_linear_scaling_min_speed=0.35 m/s`、`velocity_smoother max_velocity=[0.40,0.0,0.18]`、`deadband_velocity=[0.34,0.0,0.0]`、`adapter min_forward_drive_speed_mps=0.35`、`progress_checker required_movement_radius=0.25 m / movement_time_allowance=20.0 s`、`xy_goal_tolerance=0.35 m` 记录。阶段 4 不得把持续跟踪速度降到实车低速阻力推不动区；progress checker 放宽仅用于容忍满舵低速时 STM32 PID 起扭矩延迟，不代表允许长期顶住不动。跑车前仍必须由现场人员确认；LiDAR `/scan` 中断或驱动 `poll() timeout` 时不得继续跑车。C32 LiDAR 当前固定地址为 `192.168.1.200`，主机有线口验证地址为 `192.168.1.102/24`，阶段 3/4 跑车前必须确认 `ping -I enp0s31f6 192.168.1.200` 和 `/scan` 频率。

阶段 3/4 当前已具备离线契约检查：

```bash
python3 tools/acceptance/stage3_mapping_contract_check.py
python3 tools/acceptance/stage4_navigation_contract_check.py
```

这两个脚本不启动 ROS graph、不打开串口、不发布运动命令；它们只证明固定入口、参数、无 `Spin/BackUp`
阶段 4 行为树、topic 隔离和 adapter 数学边界符合文档。`/scan` 频率、TF 连通、地图质量、AMCL 定位、Collision Monitor 实障停车和实车到点停车
仍必须在后续上车或 rosbag replay 记录中完成。

阶段 3/4 正式测试使用固定 run id 组织证据：`YYYY-MM-DD-stageN-<site-or-map>-<case>-<HHMMSS>`。测试记录 `.md` 只写摘要、结论和证据索引；原始日志、bag、地图、截图和自动检查 JSON 按 run id 放入 `artifacts/` 或外部归档路径，不把临时调参输出混入正式记录。

每个阶段在标记最终 PASS 前，必须另开一个独立验证会话或独立 verifier 对话复核。复核会话不得发送运动命令、不得修改文件，只能读取记录、检查代码/配置、运行非运动自动检查和查看已有 rosbag/log。复核结论、证据和剩余风险必须写入阶段测试记录或最终回复；复核未通过时不得进入下一阶段。

## Ackermann bringup 与 odom 验收

当前默认 bringup 与 phase-1 验收入口分为两条路径：

- `turn_on_autoracer_robot.launch.py` 当前仍是 legacy/default 语义：`autoracer_robot` 订阅 `/cmd_vel`，发布 raw `/odom`；默认 `use_ekf:=true` 时 `autoracer_ekf.launch.py` 将 EKF 输出 remap 到 `/odom_combined`。
- legacy/default 路径可用于迁移、手动调试和旧链路兼容，但不能作为 phase-1 Ackermann 底盘或 canonical `/odom` 验收依据。
- 旧 `/cmd_vel` 不能作为正式 STM32 下行协议或阶段验收入口；当前兼容输入/桥接转换仅用于迁移、手动或 legacy 兼容。

`turn_on_autoracer_robot/launch/ackermann_chassis.launch.py` 当前默认值为：

```text
counts_per_meter:=0.0
use_ekf:=false
```

这个默认路径只用于串口、telemetry 和 `/chassis_state` bringup，不能作为阶段 1 可信 odom 验收路径。

阶段 1 odom 验收必须显式给出已标定的 `counts_per_meter>0`，并启动 `robot_localization` EKF：

```bash
ros2 launch turn_on_autoracer_robot ackermann_chassis.launch.py counts_per_meter:=<实测值> use_ekf:=true
```

验收记录必须说明 `counts_per_meter` 的来源、`/wheel_odom` 是否由 `hall_delta_count + steering_angle` 积分、`/odom` 是否由 `/wheel_odom + /imu/data` 的 EKF 输出。还必须记录 `base_footprint -> gyro_link` TF 存在、`/odom` 频率接近 `30 Hz`，以及静止时 `/odom` 不在无霍尔计数情况下持续漂移。launch 默认值改为验收路径时，同步更新本文件和根文档。

2D SLAM launch 默认 `odom_frame:=odom`，面向 phase-1 canonical `/odom`。legacy/default bringup 使用 `/odom_combined` 时，必须显式覆盖：

```bash
ros2 launch autoracer_slam_toolbox slam.launch.py odom_frame:=odom_combined
```

或先迁移到 `ackermann_chassis.launch.py counts_per_meter:=<实测值> use_ekf:=true` 的 Ackermann 验收路径。

跨层同步检查：

```bash
git diff --name-only -- README.md docs src
rg -n "<本次改动关键词>" ../README.md ../AGENTS.md ../docs README.md docs src || true
```

关键词按本次改动替换，例如 `/ackermann_cmd`、`/wheel_odom`、`/odom`、`TF`、`Nav2`、`feature/ackermann-chassis`。

## 文档与代码一致性

代码事实以当前 message、CMake、package、launch、config、URDF 和节点实现为准。文档与代码冲突时，提交中同步修正文档。
